using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MS1_MuseumSceneScript : MonoBehaviour {

    //private float timer = 0.0f;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

        //timer += Time.deltaTime * 0.25f;

        //if( timer < Mathf.PI / 2.0f )
        //    Shader.SetGlobalFloat("_UNISM_LERP", Mathf.Sin( timer ) * 0.5f + 0.5f );
        //else if (timer < Mathf.PI )
        //    Shader.SetGlobalFloat("_UNISM_WHITE_LERP", Mathf.Sin( timer - Mathf.PI / 2.0f ) * 0.5f + 0.5f);
    }
}
